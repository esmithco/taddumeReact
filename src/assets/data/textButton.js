const textButton = [
  { text: "ingresar", key: 1 },
  { text: "Crear Cuenta", key: 2 }
];

// {textButton.map(button => (
//   <ButtonLog key={button.key} text={button.text} />
// ))}

export { textButton };